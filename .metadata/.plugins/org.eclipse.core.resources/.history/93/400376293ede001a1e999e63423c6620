package com.pdfgeneration.elonik.pdf.generation.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping	
public class PdfGeneration {

	/*
	 * @RequestMapping("/abc") public String index() { return "NewFile"; }
	 * 
	 * 
	 * @PostMapping("/saveDetails") public void generatePdf() { }
	 */
	
	@GetMapping
	public String getUser() {
		return "getUser is called";
	}
	
	@PostMapping
	public String createUser() {
		return "create user is called";
	}
}
